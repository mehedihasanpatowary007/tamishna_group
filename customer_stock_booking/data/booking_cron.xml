<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="ir_cron_customer_booking_reminder" model="ir.cron">
        <field name="name">Customer Booking: Expiry Reminder</field>
        <field name="model_id" ref="model_customer_stock_booking"/>
        <field name="state">code</field>
        <field name="code">model._cron_process_reminders()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
        <field name="active">True</field>
    </record>

    <record id="ir_cron_customer_booking_expiry" model="ir.cron">
        <field name="name">Customer Booking: Auto Expiry</field>
        <field name="model_id" ref="model_customer_stock_booking"/>
        <field name="state">code</field>
        <field name="code">model._cron_expire_bookings()</field>
        <field name="interval_number">15</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>
</odoo>
